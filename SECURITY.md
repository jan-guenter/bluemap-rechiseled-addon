# Security policy

This unreleased alpha has no supported production version. Report suspected
resource-parser denial of service, namespace collision, archive traversal,
activation bypass, unsafe fallback, or publication-boundary defects privately
to the repository owner before public disclosure.

The parser is intentionally bounded to exact artifacts, a fixed 8,751-path
closure, a separate seven-path host-model ABI, 256 KiB per model, five
predicate-node levels and 14 nodes per connection predicate, exact namespaces,
and a fixed predicate vocabulary. Unknown or malformed input must deactivate
the route or atomically use stock rendering; it must never become a broader
interpreter.

Do not attach proprietary JARs, production maps, server logs, credentials,
world data, player data, or resource-pack assets to public reports.
